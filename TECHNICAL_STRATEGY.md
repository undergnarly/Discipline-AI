# 🎯 Discipline AI - Техническая стратегия и архитектура

**Версия:** 2.0  
**Дата:** Июнь 2025  
**Статус:** Утвержденная архитектура для MVP и масштабирования

---

## 1. 🏛️ Архитектурные принципы

Эти принципы — наша конституция. Они помогут принимать правильные решения в будущем.

1.  **🧠 AI-First**: Ядро продукта — это ИИ-агенты. Все технологические решения должны упрощать, а не усложнять их разработку и интеграцию.
2.  **🔌 Модульность и заменяемость**: Архитектура должна позволять легко заменять или обновлять компоненты (например, сменить модель `GPT-4` на `GPT-5` или `Claude 3.5 Sonnet`, не переписывая всю логику).
3.  **☁️ Cloud-Native & Serverless-Ready**: Начинаем с простого PaaS (Platform-as-a-Service), но архитектура должна быть готова к масштабированию в serverless-окружении (AWS Lambda, Google Cloud Functions) для оптимизации затрат.
4.  **📱 Mobile-First PWA**: Единая кодовая база для всех платформ. Фокус на легковесном и быстром мобильном опыте без необходимости проходить ревью в App Store / Google Play.
5.  **🔐 Безопасность и приватность**: Данные пользователя (календари, задачи, переписка) — это самое ценное и чувствительное. Безопасность должна быть встроена на каждом уровне.

---

## 2. 🛠️ Утвержденный технический стек

### **Backend: FastAPI (Python)**
- **Почему?**: Прямой доступ к лучшей AI/ML экосистеме (`LangChain`, `Hugging Face`). Высочайшая производительность, автоматическая валидация и документация API через `Pydantic`.
- **Ключевые библиотеки**:
    - `FastAPI`: Основа веб-сервера.
    - `Pydantic`: Валидация данных.
    - `SQLAlchemy 2.0`: Асинхронная ORM для работы с базой данных.
    - `Alembic`: Миграции схемы базы данных.
    - `Celery`: Фоновые задачи (синхронизация календаря, AI-анализ).
    - `LangChain`: Оркестрация ИИ-агентов, работа с LLM.

### **Frontend: Next.js (TypeScript)**
- **Почему?**: Лучший React-фреймворк для создания быстрых, SEO-дружелюбных и масштабируемых веб-приложений. Идеален для PWA.
- **Ключевые библиотеки**:
    - `React 18` / `Next.js 14` (App Router): Современная архитектура.
    - `TypeScript`: Строгая типизация для надежности.
    - `Tailwind CSS`: Быстрая и утилитарная стилизация.
    - `TanStack Query (React Query)`: Управление состоянием сервера, кэширование.
    - `Zustand`: Управление глобальным состоянием клиента (просто и эффективно).
    - `Framer Motion`: Плавные анимации для премиум-ощущения.

### **База данных: PostgreSQL**
- **Почему?**: Надежность "боевой" СУБД, отличная поддержка JSONB для гибких данных и мощные расширения, такие как `pgvector` (для хранения embeddings) и `TimescaleDB` (для аналитики временных рядов в будущем).
- **Альтернатива на будущее**: Для аналитических данных можно будет использовать `ClickHouse`.

### **Кэш и очереди: Redis**
- **Почему?**: Стандарт индустрии. Идеально подходит как для кэширования ответов от API (включая дорогие запросы к OpenAI), так и в качестве брокера сообщений для `Celery`.

### **AI & LLM**
- **Основная модель**: `OpenAI GPT-4` (и переход на новые версии по мере их выхода).
- **Оркестрация**: `LangChain` (Python-версия).
- **Хранение векторов**: `Pinecone` / `ChromaDB`. Начнем с ChromaDB (self-hosted), при росте перейдем на Pinecone.

### **Деплой и инфраструктура**
- **Этап 1 (MVP)**: `Railway` или `Render`. Эти PaaS-платформы позволяют развернуть весь стек (FastAPI, Next.js, PostgreSQL, Redis) за минуты, идеально для быстрой итерации.
- **Этап 2 (Масштабирование)**: `Vercel` для Frontend + `AWS` / `Google Cloud` для Backend.
    - Бэкенд можно будет упаковать в контейнеры (`Docker`) и запустить на `AWS Fargate` или `Google Cloud Run` для serverless-масштабирования.

---

## 3. 🔮 Стратегия будущего расширения (Future-Proofing)

Как мы обеспечим, чтобы наша архитектура не устарела через год?

### **Принцип №1: Абстрагирование от конкретной ИИ-модели**
Мы не будем делать прямые вызовы `openai.ChatCompletion.create()` в бизнес-логике. Вместо этого создадим внутренний сервис-адаптер.

```python
# services/llm_service.py

class LLMProvider:
    async def get_completion(self, prompt: str, model_name: str) -> str:
        # Здесь логика выбора провайдера (OpenAI, Anthropic, etc.)
        pass

# В коде агентов будет так:
# completion = await llm_service.get_completion(prompt, model_name="gpt-4-turbo")
# А не так:
# completion = await openai.ChatCompletion.create(...)
```
**Выгода**: Когда выйдет `GPT-5` или `Claude 4`, нам нужно будет изменить только `LLMProvider`, а не десятки файлов с агентами. Мы сможем даже динамически выбирать лучшую модель для конкретной задачи.

### **Принцип №2: Модульная архитектура агентов**
Каждый ИИ-агент будет самодостаточным модулем со своим набором инструментов (`Tools` в терминах `LangChain`).

- **Агент Календаря**: Имеет инструменты `get_events`, `create_event`, `find_free_slot`.
- **Агент Почты**: Имеет инструменты `read_emails`, `draft_reply`, `archive_email`.

**Выгода**:
- **Добавление нового агента** (например, `Finance Agent`) не затронет существующих. Мы просто создаем новый модуль и новый набор инструментов.
- **Обновление инструмента**: Если Google Calendar API изменится, нам нужно обновить только один инструмент, а не всего агента.

### **Принцип №3: Микросервисы по необходимости, а не по умолчанию**
Начинаем с "модульного монолита" — единого FastAPI-приложения, но с четко разделенной логикой по сервисам (`services/calendar.py`, `services/ai.py` и т.д.).

**Выгода**:
- **Простота на старте**: Не нужно управлять сложной сетью микросервисов.
- **Легкое выделение**: Если в будущем какой-то сервис (например, анализ паттернов поведения) станет очень ресурсоемким, его можно будет легко выделить в отдельный микросервис, так как код уже изолирован.

### **Принцип №4: Данные — это наш актив**
Каждое взаимодействие пользователя с ИИ — это ценная информация.
- **"👍 / 👎" на предложениях ИИ**: Мы будем собирать эти данные.
- **Редактирование текста, сгенерированного ИИ**: Мы будем это отслеживать.

**Выгода**: Собрав достаточно данных, мы сможем **дообучать (fine-tune) более дешевые открытые модели** (например, из Hugging Face) для выполнения специфичных задач. Это позволит в будущем значительно сократить расходы на API OpenAI и повысить качество рекомендаций.

---

## 4. 🚀 План действий на первую неделю (новый, переосмысленный)

С этой архитектурой в уме, план на первую неделю остается похожим, но теперь мы точно знаем, *почему* мы делаем каждый шаг.

**Цель недели:** Создать "скелет" приложения, подтверждающий работоспособность всех ключевых компонентов стека.

1.  **Backend**:
    - Настроить FastAPI с `Pydantic` и `SQLAlchemy`.
    - Создать `User` модель и эндпоинты для JWT аутентификации.
    - Создать `CalendarEvent` модель и *пустые* CRUD эндпоинты (только структура, без логики).
    - **Ключевой результат**: работающий API с документацией Swagger, где можно зарегистрироваться и получить токен.

2.  **Frontend**:
    - Настроить Next.js с `TypeScript` и `Tailwind`.
    - Создать страницы входа/регистрации и пустой дашборд.
    - Настроить API-клиент для взаимодействия с бэкендом и систему защищенных роутов.
    - **Ключевой результат**: Пользователь может зарегистрироваться и войти через UI, после чего попадает на защищенную страницу дашборда.

3.  **Инфраструктура**:
    - Подготовить `docker-compose.yml` для локальной разработки (FastAPI + PostgreSQL + Redis).
    - **Ключевой результат**: Любой разработчик может одной командой `docker-compose up` запустить все окружение.

Этот план позволяет нам за неделю проверить, что все части стека работают вместе, и создает прочную основу для дальнейшей быстрой разработки. 