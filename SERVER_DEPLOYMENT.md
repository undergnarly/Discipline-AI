# 🚀 Быстрый старт развертывания на сервере 64.225.113.174

## 📋 Что нужно сделать на ЛОКАЛЬНОЙ машине:

### 1. Создать GitHub репозиторий и запушить код

```bash
cd /home/muvs/pets/discipline-ai-new

# Добавить все в git
git add .

# Коммит
git commit -m "Initial commit"

# Создай репозиторий на GitHub: https://github.com/new
# Назови его "discipline-ai"

# Добавь remote (замени ТВОЙ_ЮЗЕРНЕЙМ на свой)
git remote add origin https://github.com/ТВОЙ_ЮЗЕРНЕЙМ/discipline-ai.git

# Запуш
git branch -M main
git push -u origin main
```

---

## 🖥️ Что нужно сделать на СЕРВЕРЕРА:

### 1. Подключиться к серверу

```bash
ssh root@64.225.113.174
```

### 2. Скопировать файлы настройки с локальной машины на сервер

**На локальной машине (в новом терминале):**

```bash
# Скопировать скрипты настройки на сервер
scp /home/muvs/pets/discipline-ai-new/*.sh root@64.225.113.174:/root/

# Или если скрипты уже в git, то просто клонировать репозиторий
```

### 3. Запустить установку на сервере

**На сервере (после подключения по SSH):**

```bash
# Шаг 1: Начальная настройка сервера (установка ПО)
cd /root
chmod +x setup-server.sh
./setup-server.sh

# Шаг 2: Клонировать репозиторий (замени на свой GitHub URL)
cd /var/www
git clone https://github.com/ТВОЙ_ЮЗЕРНЕЙМ/discipline-ai.git
cd discipline-ai

# Шаг 3: Настроить backend
chmod +x init-backend.sh
./init-backend.sh

# Шаг 4: Настроить frontend
chmod +x init-frontend.sh
./init-frontend.sh

# Шаг 5: Настроить Nginx
chmod +x setup-nginx.sh
./setup-nginx.sh
```

### 4. Проверить что все работает

```bash
# Проверить backend
systemctl status discipline-ai-backend
curl http://localhost:8000/health

# Проверить frontend
pm2 status

# Проверить nginx
systemctl status nginx
```

### 5. Открыть порты в firewall (если нужно)

```bash
ufw status
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 22/tcp
```

---

## 🌐 Проверить в браузере

Открой в браузере:
- **http://64.225.113.174** - фронтенд
- **http://64.225.113.174/api/docs** - API документация
- **http://64.225.113.174/health** - проверка backend

---

## 🔐 Настроить SSL (HTTPS) - Опционально

```bash
# Если у тебя есть домен, настроь SSL
certbot --nginx -d твойдомен.com -d www.твойдомен.com
```

---

## 🔄 Как обновлять приложение на сервере

**На локальной машине:**

```bash
# Внести изменения
git add .
git commit -m "New feature"
git push
```

**На сервере:**

```bash
ssh root@64.225.113.174
cd /var/www/discipline-ai
git pull

# Перезапустить сервисы
systemctl restart discipline-ai-backend
cd frontend && npm run build && pm2 restart discipline-ai-frontend
```

**Или использовать деплой скрипт с локальной машины:**

```bash
cd /home/muvs/pets/discipline-ai-new
./deploy.sh
```

---

## 📊 Мониторинг на сервере

```bash
# Логи backend
journalctl -u discipline-ai-backend -f

# Логи frontend
pm2 logs discipline-ai-frontend

# Логи nginx
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log

# Статус всех сервисов
systemctl status discipline-ai-backend
pm2 status
systemctl status nginx
```

---

## ⚠️ Важные замечания

1. **Безопасность:**
   - Измени `SECRET_KEY` в `/var/www/discipline-ai/backend/.env`
   - Настрой SSH ключи вместо паролей
   - Ограничь доступ по SSH

2. **API ключи:**
   - Добавь реальные ключи в `.env` файлы:
     - Google OAuth (CLIENT_ID, SECRET)
     - OpenAI API Key
     - Anthropic API Key
     - Supabase credentials

3. **База данных:**
   - Для production лучше использовать PostgreSQL вместо SQLite
   - Настрой регулярные бэкапы

4. **Домен:**
   - Добавь A запись в DNS настроек домена:
     - Type: A
     - Name: @
     - Value: 64.225.113.174

---

## 📝 Что было создано:

✅ **Файлы для деплоя:**
- `setup-server.sh` - начальная настройка сервера
- `init-backend.sh` - инициализация backend
- `init-frontend.sh` - инициализация frontend
- `setup-nginx.sh` - настройка Nginx
- `deploy.sh` - скрипт для деплоя с локальной машины

✅ **Документация:**
- `DEPLOYMENT.md` - подробная инструкция по развертыванию
- `SERVER_DEPLOYMENT.md` - эта инструкция

✅ **Git:**
- `.gitignore` настроен
- Git репозиторий инициализирован

---

**Готово к развертыванию! 🚀**

Следуй инструкциям выше и все будет работать!
