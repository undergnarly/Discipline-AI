# 🗺️ Структура проекта Discipline AI

Этот документ — живая карта нашего проекта. Он описывает назначение каждого важного файла и директории.

## 📂 Корень проекта

-   `docker-compose.yml`: Оркестрирует запуск всех сервисов (backend, frontend, db, redis) для локальной разработки.
-   `TECHNICAL_STRATEGY.md`: Наша техническая конституция. Описывает архитектуру, стек и принципы.
-   `PROJECT_STRUCTURE.md`: **(Этот файл)** Карта проекта.
-   `TASK_TRACKING.md`: План и трекер задач.
-   `.cursor-rules.json`: Правила для AI-ассистента.
-   `backend/`: Директория с кодом бэкенда (FastAPI).
-   `frontend/`: Директория с кодом фронтенда (Next.js).

---

## 🐍 `backend/` - Бэкенд (FastAPI)

-   `Dockerfile`: Инструкции по сборке Docker-образа для бэкенда.
-   `pyproject.toml`: Описание проекта и его зависимостей (управляется `Poetry`).
-   `poetry.lock`: Файл с точными версиями всех зависимостей.
-   `alembic/`: Директория для миграций базы данных (`Alembic`).
-   `tests/`: Директория с тестами (`pytest`).

### 🐍 `backend/app/` - Основной код приложения

-   `main.py`: Точка входа в приложение FastAPI. Здесь инициализируется приложение, подключаются роутеры и middleware.
-   `config.py`: Конфигурация приложения. Загружает настройки из `.env` файла с помощью `Pydantic`.
-   `database.py`: Настройка подключения к базе данных (`SQLAlchemy`).
-   `dependencies.py`: FastAPI зависимости, используемые в разных частях приложения (например, `get_current_user`).

#### `backend/app/api/` - Роутеры API

-   `auth.py`: Эндпоинты для аутентификации (регистрация, логин, JWT, Google OAuth).
-   `calendar.py`: CRUD эндпоинты для событий календаря.
-   *В будущем: `agents.py`, `journal.py`...*

#### `backend/app/models/` - Модели данных SQLAlchemy

-   `__init__.py`: Обеспечивает видимость моделей для Alembic.
-   `user.py`: Модель `User`.
-   `calendar.py`: Модель `CalendarEvent`.

#### `backend/app/schemas/` - Схемы данных Pydantic

-   `user.py`: Схемы для валидации данных пользователя (`UserCreate`, `UserPublic`).
-   `token.py`: Схемы для JWT токенов.
-   `calendar.py`: Схемы для событий календаря.

#### `backend/app/services/` - Бизнес-логика

-   `user_service.py`: Логика для работы с пользователями (создание, поиск).
-   `google_service.py`: Логика для взаимодействия с Google API.
-   *В будущем: `calendar_service.py`, `ai_planning_service.py`...*

#### `backend/app/utils/` - Вспомогательные утилиты

-   `security.py`: Функции для хеширования паролей и работы с JWT.

---

## ⚛️ `frontend/` - Фронтенд (Next.js)

-   `Dockerfile`: Инструкции по сборке Docker-образа для фронтенда.
-   `package.json`: Список зависимостей и скриптов `npm`.
-   `next.config.js`: Конфигурация Next.js.
-   `tailwind.config.ts`: Конфигурация `Tailwind CSS`.
-   `tsconfig.json`: Конфигурация `TypeScript`.

### ⚛️ `frontend/src/`

-   `app/`: Основная директория для страниц и компонентов в Next.js App Router.
    -   `layout.tsx`: Корневой layout приложения.
    -   `page.tsx`: Главная страница (лендинг).
    -   `(auth)/`: Группа роутов для аутентификации.
        -   `layout.tsx`: Общий layout для страниц логина и регистрации.
        -   `login/page.tsx`: Страница входа.
        -   `register/page.tsx`: Страница регистрации.
    -   `dashboard/`: Защищенная группа роутов для основного приложения.
        -   `layout.tsx`: Layout дашборда с боковой панелью.
        -   `page.tsx`: Главная страница дашборда.
    -   `calendar/`: Страницы, связанные с календарем.
        -   `page.tsx`: Основная страница календаря.
-   `lib/`: Вспомогательные модули и сервисы.
    -   `api.ts`: Настроенный клиент `axios` для запросов к бэкенду.
    -   `auth_service.ts`: Функции для логина, регистрации, логаута.
-   `components/`: Переиспользуемые React-компоненты (кнопки, инпуты и т.д.).
-   `hooks/`: Кастомные React-хуки (например, `useUser`).
-   `types/`: Определения `TypeScript` типов (`User`, `Token`).
-   `styles/`: Глобальные стили. 